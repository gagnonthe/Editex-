services:
  - type: static_site
    name: editex-web
    rootDir: frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    autoDeploy: true
    envVars:
      - key: VITE_BACKEND_URL
        sync: false
